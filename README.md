# FPS Targeter v2.3

![Version](https://img.shields.io/badge/version-2.3-blue.svg)
![Platform](https://img.shields.io/badge/platform-Windows-lightgrey.svg)
![Python](https://img.shields.io/badge/python-3.8+-blue.svg)
![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)

Интеллектуальный трекер FPS и оптимизатор производительности для Windows.

FPS Targeter — это мощный инструмент для мониторинга и оптимизации производительности игр. Приложение автоматически обнаруживает оборудование, отслеживает FPS в реальном времени и предлагает оптимальные настройки для максимальной производительности.

## 📦 Содержание

- [Возможности](#возможности)
- [Системные требования](#системные-требования)
- [Установка](#установка)
- [Быстрый старт](#быстрый-старт)
- [Использование](#использование)
- [Сборка в .exe](#сборка-в-exe)
- [FAQ / Часто задаваемые вопросы](#faq--часто-задаваемые-вопросы)
- [Файлы проекта](#файлы-проекта)
- [Лицензия](#лицензия)

## 🌟 Возможности

- 🖥️ Автоматическое обнаружение оборудования (CPU, GPU, RAM, VRAM)
- 📊 Реальное отображение FPS и нагрузки на систему
- 🎮 Интеллектуальный подбор настроек игр
- 🌩️ Облачная синхронизация настроек между устройствами
- ⚡ Автоматическая оптимизация производительности
- 🔧 Поддержка пользовательских профилей

## 💻 Системные требования

- Windows 10/11 (64-bit)
- Python 3.8+ (для запуска из исходников)
- 4 ГБ RAM (минимум)
- 1 ГБ свободного места на диске

## 🚀 Установка

### С помощью готового .exe файла (рекомендуется)

1. Скачайте готовый `FPS_Targeter.exe` из папки `src/dist/`.
2. Убедитесь, что в папке `src/dist/` также находятся файлы конфигурации:
   - `game_requirements.json`
   - `hardware_benchmark.json`
   - `overlay_settings.json`
3. Запустите его без установки. Дополнительная информация доступна в `START.md` (находится в корне проекта).

### С помощью исходного кода

1. Убедитесь, что у вас установлен Python 3.8 или выше.
2. Скачайте исходный код.
3. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
4. Запустите приложение:
   ```bash
   cd src
   python main.py
   ```

## 🎮 Быстрый старт

1. Запустите `FPS_Targeter.exe` (из `src/dist/`) или `python main.py` (из `src/`).
2. Программа автоматически обнаружит ваше оборудование.
3. Создайте или выберите профиль.
4. Начните игру — FPS Targeter будет работать в фоновом режиме.

## 📋 Использование

### Горячие клавиши

Вы можете использовать следующие комбинации для управления во время игры:

| Клавиши | Действие |
|---------|----------|
| `Alt+F` | Переключить отображение FPS |
| `Ctrl+Shift+R` | Сбросить настройки |
| `Ctrl+Shift+F` | Переключить FPS |

## 🔧 Сборка в .exe

### Способ 1: Простая сборка (рекомендуется)

```bash
cd src
simple_build.bat
```

### Способ 2: Полная сборка

```bash
cd src
build.bat
```

### Способ 3: Ручная сборка

```bash
cd src
pip install pyinstaller
pyinstaller --onefile --windowed --clean main.py
```

**Важно**: После сборки файлы `.json` не копируются автоматически PyInstaller'ом. Выполните ручное копирование:

```powershell
Copy-Item -Path "game_requirements.json", "hardware_benchmark.json", "overlay_settings.json" -Destination "dist\" -Force
```

Готовый .exe файл будет находиться в папке `src/dist/`.

**Содержимое папки dist/ после сборки:**
- `FPS_Targeter.exe` (86 MB) - главный исполняемый файл
- `game_requirements.json` (205 байт) - база требований игр
- `hardware_benchmark.json` (4086 байт) - база бенчмарков
- `overlay_settings.json` (150 байт) - настройки оверлея

## ❓ FAQ / Часто задаваемые вопросы

- **Q: При запуске из исходников возникают проблемы с правами доступа (wmi / pywin32).**
  - **A:** Для корректного сбора информации о системе библиотекам `wmi` и `pywin32` могут потребоваться права администратора. Попробуйте запустить консоль от имени Администратора.
- **Q: Появляются ошибки, связанные с pynvml. Я не использую видеокарту от NVIDIA.**
  - **A:** Библиотека `pynvml` предназначена для работы с видеокартами NVIDIA. Если у вас видеокарта другого производителя (AMD, Intel), в логах могут появляться предупреждения об отсутствии совместимого драйвера. Это нормальное поведение.
- **Q: Что происходит при первом запуске .exe файла?**
  - **A:** При первом запуске в папке `src/dist/` автоматически создадутся служебные файлы: `fps_data.db`, `fps_settings.json`, `fps_targeter.log`, `game_presets_db.json`. Это нормально, данные файлы используются для локального хранения настроек и логов.

## 📁 Файлы проекта

### Основные файлы

- `src/main.py` - главный файл запуска приложения
- `requirements.txt` - список зависимостей Python (в корневой папке)
- `START.md` - подробное руководство по запуску

### Файлы конфигурации

```
src/
├── game_requirements.json      - Системные требования игр
├── hardware_benchmark.json     - База данных производительности
├── icon.ico                    - Иконка приложения
└── overlay_settings.json       - Настройки оверлея
```

### Скрипты сборки

- `src/simple_build.bat` - простая сборка в .exe (рекомендуется)
- `src/build.bat` - полная сборка с очисткой
- `src/build.sh` - сборка для Linux/Mac (требуется Wine)

## 📄 Лицензия

MIT License

Copyright (c) 2026 FPS Targeter Developer

## 👨‍💻 Автор

Разработано с помощью GigaCode

## 🙏 Благодарности

Спасибо за использование FPS Targeter! Если у вас есть вопросы или предложения, пожалуйста, свяжитесь с разработчиком.
